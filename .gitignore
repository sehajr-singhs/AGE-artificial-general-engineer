# node / runtime
node_modules/
.age/
.demo/
journal.jsonl

# python
__pycache__/
*.pyc
.venv/
venv/

# heavy artifacts (checkpoints, logs) — JSON results are committed
*.pt
*.log
*.err
*.aux
*.out
*.toc
*.bbl
*.blg

# OS
.DS_Store
Thumbs.db

# (component papers live in their own repos; excluded via .git/info/exclude
#  so they stay out of this repo without hiding them from tooling)
